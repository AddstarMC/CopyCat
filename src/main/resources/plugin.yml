name: CopyCat
version: ${plugin.version}
api-version: ${spigot.api.version}
main: au.com.addstar.copycat.CopyCatPlugin
depend: [Minigames, Monolith]

permissions:
  copycat.command.create:
    default: OP
  copycat.command.set:
    default: OP
  copycat.command.delete:
    default: OP
  copycat.command.editor:
    default: OP

commands:
  copycat: